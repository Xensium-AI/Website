# XENSIUM AI website — environment variables
# Copy to .env.local and fill in real values. Placeholder values only here —
# never commit real secrets.

# Production domain, used to resolve absolute Open Graph / social URLs.
# Example: https://www.your-domain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Future integrations (not connected yet) ---
# When the contact form backend / email service is added, its credentials
# belong in .env.local (gitignored), e.g.:
# EMAIL_SERVICE_API_KEY=your-key-here
